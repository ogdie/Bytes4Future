//Cria a função obtemPropriedadeDeObjeto que recebe dois argumentos, objeto, do tipo objeto,
// e propriedade, do tipo string, e retorna a propriedade do objeto

function obtemPropriedadeDeObjeto(objeto, propriedade) {
    return objeto[propriedade];
}