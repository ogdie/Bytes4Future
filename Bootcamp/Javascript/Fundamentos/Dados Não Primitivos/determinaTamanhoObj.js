//Cria a função  que recebe um argumento do tipo objeto e retorna 
// o tamanho (quantidade de propriedades) do objeto.
function determinaTamanhoDeObjeto(objeto) {
    return Object.keys(objeto).length;
}