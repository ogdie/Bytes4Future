//Cria uma função que recebe um array como argumento e retorna um novo array com os elementos ordenados do menor ao maior.
function ordenaDoMenorAoMaior(array){
    return array.sort((a, b) => a - b);
}