//Cria uma função que recebe um array e um número como argumentos e retorna um array com os elementos que são menores que n.
function filtraMenoresQueN(array, n){
    return array.filter(menores => menores < n);
}