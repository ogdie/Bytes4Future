//Cria uma função que recebe um array e um número como argumentos e retorna um array com cada número multiplicado por n.
function multiplicadosPorN(array, n){
    return array.map(multiplicador => multiplicador * n);
}